#include <bits/stdc++.h>

int main()
{
    int n{};
    std::cin>>n;


    int sum{};
    std::cin>>sum;


    std::vector<int> arr(n);

    for(int i{};i<n;i++)
    {
        std::cin>>arr[i];
    }
}