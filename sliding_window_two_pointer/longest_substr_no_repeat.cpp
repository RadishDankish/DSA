#include <bits/stdc++.h>

int main()
{
    std::string a;
    std::cin>>a;

    int i{};
    int j{};

    std::vector<int> hash(26);

    for(int k{};k<a.size();k++)
    {
        if(hash[int(a[k])-97]==1)
        {
            
        }
    }

}