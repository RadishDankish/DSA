#include <bits/stdc++.h>


int partition(std::vector<int> &arr,int lo, int hi)
{
    if(lo>=hi)
    {
        return;
    }
    else
    {
        int i{1};
        int j{hi};
        int pivot{arr[1]};

        while(i<)
        {
            if()
        }
    }
}

void quicksort(std::vector<int> &arr, int n)
{

}

int main()
{
    int n{};
    std::cin>>n;


    std::vector<int> a(n);

    for(int i{};i<n;i++)
    {
        std::cin>>a[i];
    }






    for(int i{};i<n;i++)
    {
        std::cout<<a[i]<<" ";
    }
}