#include <bits/stdc++.h>

int main()
{
    std::string a;
    std::cin>>a;

    int n{a.size()};
    int sign_flag{};
    int i{};

    if(a[0]=='-')
    {   
        i=1;
        sign_flag=1;
    }
    else if(a[0]=='+')
    {
        i=1;
        sign_flag=0;
    }
    
    int start_flag{};

    for(;i<n;i++)
    {
        if(a[i]=='0')
        {
            
        }
    }
}